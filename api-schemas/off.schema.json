{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "/off request",
  "type": "object",
  "properties": {
    "off": { "type": "boolean" }
  },
  "required": ["off"],
  "description": "POST request. Turns the device off. Response is a generic API code object."
}
