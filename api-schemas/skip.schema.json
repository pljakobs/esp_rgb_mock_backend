{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "API Skip Command Schema",
  "description": "Schema for /skip endpoint, based on JsonProcessor::onSkip expectations.",
  "type": "object",
  "properties": {
    "channels": {
      "type": "array",
      "items": { "type": "integer" }
    },
    "relay": { "type": "boolean" }
  },
  "required": ["channels"]
}
