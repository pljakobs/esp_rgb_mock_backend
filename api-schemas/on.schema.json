{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "/on request",
  "type": "object",
  "properties": {
    "on": { "type": "boolean" }
  },
  "required": ["on"],
  "description": "POST request. Turns the device on. Response is a generic API code object."
}
